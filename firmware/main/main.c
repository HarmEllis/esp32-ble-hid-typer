#include <stdio.h>
#include "esp_log.h"

static const char *TAG = "main";

void app_main(void)
{
    ESP_LOGI(TAG, "ESP32 BLE HID Typer starting...");
    ESP_LOGI(TAG, "Firmware skeleton - Phase 1 complete");
}
